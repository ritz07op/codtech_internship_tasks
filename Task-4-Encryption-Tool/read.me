# Task 4 – Advanced Encryption Tool

A Python-based tool to securely encrypt and decrypt files using **AES-256** encryption via the `cryptography` library (Fernet module).

## 💡 Features
- Encrypts any file using AES-256
- Automatically saves and loads a secret key (`secret.key`)
- Decrypts `.enc` files back to original form
- Simple command-line interface (CLI)

## ▶️ How to Run
1. Install required library:
```bash
pip install cryptography
